apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx
  # - name: alpine
  #   image: alpine
  #   command:
  #   - sleep
  #   - "3600"
  #   resources:
  #     limits:
  #       memory: "10Mi"
  #       cpu: "100m"